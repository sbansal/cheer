document.getElementById('transaction-details').innerHTML = "<%= j (render @transaction) %>"
var txRow = document.getElementById("tx-<%= @transaction.id %>")
if (txRow) {
  txRow.innerHTML = "<%= j (render 'transaction_summary', transaction: @transaction) %>"
}
