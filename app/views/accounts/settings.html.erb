<% title "Settings" %>
<div class="row border-bottom pb-4 mb-4">
  <div class="col-sm-4 mb-3">
    <div class="fw-bold">Profile</div>
    <div class="grey-500 med pe-5">You can update all your personal settings here.</div>
  </div>
  <div class="col-sm-8">
    <%= render partial: 'users/profile_form'%>
  </div>
</div>
<div class="row border-bottom pb-4 mb-4">
  <div class="col-sm-4 mb-3">
    <div class="fw-bold">Security</div>
    <div class="grey-500 med pe-5">Setup two factor authentication to add an additional layer of security.</div>
  </div>
  <div class="col-sm-8">
    <div class="row" id="tfa-container">
      <%= render partial: 'two_factor_authentication/setting'%>
    </div>
  </div>
</div>
<div class="row border-bottom pb-4">
  <div class="col-sm-4 mb-3">
    <div class="fw-bold">Manage your family accounts</div>
    <div class="grey-500 med pe-5">Here is everyone that has access to this account.</div>
  </div>
  <div class="col-sm-8">
    <ul class="basic">
      <% current_user.account.users.each do |user| %>
        <%= render 'users/user_info', user: user %>
      <% end %>
    </ul>
    <section class="form-background">
      <div class="fw-bold med">Want to add members?</div>
      <%= render partial: 'users/invite_person_form' %>
    </section>
  </div>
</div>

